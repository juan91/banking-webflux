server:
  port: 8081
spring:
  application:
    name: "OpenBox"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
  data:
    mongodb:
      uri: mongodb+srv://admin:I6vE5tmbEU5u4sI8@galeras.vjcyxol.mongodb.net/banking
  rabbitmq:
    host: possum.lmq.cloudamqp.com
    port: 5671
    username: ltqlmrty
    password: I9geTinkznSEzhMLw0SI7VTD0ERfuXJW
    virtual-host: ltqlmrty
    ssl:
      enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
